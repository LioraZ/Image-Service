@model ImageServiceWeb.Models.ImageWeb.ImageWebModel
@{
    ViewBag.Title = "Index";
}

<h2>Home</h2>
This is my firsts page!!WooHooooo!!!!
<p>
    @Html.DisplayNameFor(model =>model.status)
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Students)
        </th>
        <th>
            @Html.DisplayName("Last Name")
        </th>
        <th>
            @Html.DisplayName("StudentID")
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model.Students)
    {
        <tr>
            <td>
                @Html.DisplayNameFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayNameFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayNameFor(modelItem => item.StudentID)
            </td>
        </tr>
    }

</table>